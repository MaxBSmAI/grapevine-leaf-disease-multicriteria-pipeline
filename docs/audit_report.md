# Audit report provenance

The full audit report is maintained with the study records. This public
repository contains the executable audit code and its schema; regenerate the
report locally from the dataset using the commands in `docs/RUNBOOK.md`.
